# Café Fausse Web Application

A full-stack restaurant website built with **React**, **Flask**, and **MySQL**.

## Features
- Responsive UI built with React + custom CSS (Flexbox and Grid)
- Reservation system with backend validation
- Newsletter subscription form
- Menu, About, Gallery, and Home pages
- Integrated with MySQL for persistent data

## Technologies
- **Frontend:** React (Vite)
- **Backend:** Flask (Python)
- **Database:** MySQL
- **Styling:** Pure CSS with Flexbox and Grid
- **Deployment:** Local (Flask + Vite)

## Setup Instructions

### Backend
```bash
cd backend
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
flask run
